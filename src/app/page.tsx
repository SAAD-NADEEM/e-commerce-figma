import HeroWrapper from "./components/HeroSection/HeroWrapper";

function Home() {

    return (
        <main className="flex flex-col items-center">
            <HeroWrapper />
        </main>
    );
};

export default Home;